# 家人共同測試遊戲的流程

本文整理：**準備雲端環境 → 每位家人建立帳號 → 安裝測試版 → 用 Join Code 配對 → 確認各人存檔／每日挑戰獨立**。之後若程式或資料表有更新，請以倉庫內最新腳本與 migration 為準。

---

## 一、準備者（你）要先完成的事

### 1. Supabase 專案

1. 在 [Supabase Dashboard](https://supabase.com/dashboard) 建立或使用既有專案。
2. **Authentication → Providers**：開啟 **Email**（家人用信箱註冊／登入）。若開啟「信箱驗證」，家人須先收信再登入。
3. **SQL**：依序執行（或在本機用 CLI `supabase db push`）下列 migration（路徑以倉庫為準）：
   - `Backend/supabase/migrations/20260511090000_design_data_rows.sql`（若要用雲端企劃表）
   - `Backend/supabase/migrations/20260512100000_player_state_join_lobbies.sql`（玩家資料、Join Code、`daily_hunt_records` 等）
   - `Backend/supabase/migrations/20260512103000_daily_hunt_upsert_rpc.sql`（每日狩獵 RPC upsert）
4. **Project Settings → API**：複製 **Project URL**、**Publishable（anon）key**；僅在你本機／CI 使用 **Secret（service_role）**，勿給家人或打進遊戲安裝檔。

### 2. 企劃資料（可選）

若遊戲從雲端讀企劃：在本機設定 `.env` 的 `SUPABASE_SERVICE_ROLE_KEY` 後執行 `npm run import-design`（詳見專案 `package.json`）。家人端只需 **anon**，不需 service_role。

### 3. Unity 建置設定（交付前）

1. 在 Unity 建立 **Supabase Runtime Config** 資產（選單：**Monster Hunter → Supabase Runtime Config**），填入 **URL** 與 **anon key**。
2. 將該資產指派給會連線的元件（例如 **NetworkManager**、**EmailPasswordAuth**、**PlayerStateCloudRepository**）。
3. 建置 **Android APK**（或你們約定的平台），將 APK 透過可信管道傳給家人（雲端硬碟、通訊軟體等）；勿公開散佈含金鑰的開發版設定檔。

---

## 二、每位家人要做的事

### 1. 註冊與登入

1. **各自使用不同信箱**註冊（同一 Supabase 專案）。勿共用同一帳號，否則存檔與每日挑戰會混在一起。
2. 可選：用瀏覽器開啟倉庫內 **`Web/family-auth/index.html`**（填入 URL + anon）先做信箱／密碼測試；正式仍以 **遊戲内 EmailPasswordAuth** 為主。
3. 登入成功後，遊戲會取得 **自己的 user id（auth.uid）**，存檔與 **`daily_hunt_records`** 都以該身分隔離。

### 2. 安裝測試版

1. 安裝你提供的 APK（Android 可能需在系統設定允許「安裝未知來源」）。
2. 首次開啟後完成登入；**勿**預期 `PlayerState/` 底下本機 JSON 為權威資料——正式流程以 **Supabase** 為準（見 `PlayerState/README.txt`）。

---

## 三、兩人一起測「連線／Join Code」的流程

> 說明：目前架構是 **Supabase 上建立／加入 Join Code 房間**；實際 **P2P 對打連線**仍須遊戲內網路層（Host–Client）另接。以下為「配對成功」前後的建議流程。

### Host（開房）

1. 登入遊戲。
2. 呼叫 **建立 Join Code**（對應 RPC `create_join_lobby`，由 **NetworkManager** 協程驅動）。
3. 將畫面上 **6 碼**告訴 Guest（電話／訊息即可）。

### Guest（加入）

1. 登入遊戲。
2. 輸入 Host 給的代碼，執行 **加入**（`peek_open_lobby_by_code` / `join_lobby_by_code`）。
3. 成功後，雙方在資料庫會對應到同一筆 **join_lobbies**（host / guest）。後續再用你們實作的 P2P／Relay 依 `host_user_id`、`guest_user_id` 建立連線。

### 網路環境建議（家人測試）

- **同一 Wi‑Fi**：最容易成功，延遲低。
- **不同地方／4G**：可能需要 NAT 穿透或中繼；若尚未實作，請先以同區網測試。

---

## 四、確認「每人資料與每日挑戰分開」

| 項目 | 說明 |
|------|------|
| **存檔／Profile** | `profiles` 以 `id = auth.users.id` 區分；每人登入自己的帳號即可。 |
| **倉庫／圖鑑** | `warehouse_items`、`player_encyclopedia_progress` 使用 **owner_id**，RLS 限制只能存取自己的列。 |
| **每日狩獵** | `daily_hunt_records` 唯一鍵為 **(owner_id, record_date, daily_quest_id)**；遊戲應呼叫 **`upsert_daily_hunt_record` RPC**（見 `PlayerStateCloudRepository`），兩人挑戰次數各自計算。 |

測試方式：A、B 各登入後各完成一次每日條件，到 Supabase **Table Editor** 檢查 **`daily_hunt_records`** 是否各有 **兩列 owner_id**。

---

## 五、常見問題（簡要）

- **登入失敗**：檢查信箱是否需驗證、密碼是否正確、Supabase Auth 是否開啟 Email。
- **Join Code 無效**：代碼過期、房間已滿、或 Host 未成功開房；Host 可重新建立代碼。
- **看不到對方／無法連線**：Join Code 只負責「配對身分」；若尚未接 P2P，屬預期現象，請先確認配對 RPC 成功再開發連線層。
- **SQL Editor 跳出「destructive operations」**：通常因有 `DROP TRIGGER` 等語句，屬確認視窗；確認環境無誤後可執行（詳見 migration 說明）。

---

## 六、之後你要「再處理」時可對照的文件位置

| 主題 | 路徑或檔案 |
|------|------------|
| Join Code（RPC、表） | `Backend/supabase/migrations/20260512100000_player_state_join_lobbies.sql` |
| 每日狩獵 RPC | `Backend/supabase/migrations/20260512103000_daily_hunt_upsert_rpc.sql` |
| Unity：連線入口 | `GameClient/Assets/Scripts/Network/NetworkManager.cs` |
| Unity：登入 | `GameClient/Assets/Scripts/Core/EmailPasswordAuth.cs` |
| Unity：雲端每日紀錄 | `GameClient/Assets/Scripts/Core/PlayerStateCloudRepository.cs` |
| 網頁快速登入測試 | `Web/family-auth/index.html` |

---

文件版本：依倉庫目前結構撰寫；若流程或 UI 變更，請更新本檔對應段落。
