# =============================================================================
# Supabase（複製此檔為 .env 後填入；不要將 .env 提交到 Git）
# Dashboard：Project Settings → API → Project URL、anon public key
# 文件：https://supabase.com/dashboard/project/_/settings/api
# =============================================================================

# 雲端專案（一般本機開發連線用）
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_ANON_KEY=your_anon_key_here

# 若僅使用「本機 Docker Supabase」（執行 start-dev.bat 或 npm run supabase:start 成功後），可改成下列並執行：
#   npm run supabase:status
# 以取得實際的 Publishable／anon key（預設 API 為 http://127.0.0.1:54321）。
# SUPABASE_URL=http://127.0.0.1:54321
# SUPABASE_ANON_KEY=請由 supabase status 複製
#
# 將上述 URL／anon 同步到 Unity（Resources/SupabaseRuntimeConfig.asset）：
#   npm run sync:unity-env

# 僅供本機除錯 Edge Functions／遷移／匯入 DesignData 腳本；絕對不可放入遊戲安裝檔
# 須為 Dashboard → API 的 Secret（sb_secret_…）或 Legacy「service_role」長 JWT（eyJ…）；勿用 Publishable
# SUPABASE_SERVICE_ROLE_KEY=
